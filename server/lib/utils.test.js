const axios = require('axios')

module.exports = {
    function1(data){
        let messagesCopy = []
        if(data.user){
            messagesCopy.push(data.user + ' has left the room.')
        }
    }
    function2(){
        
    }
}